{
  "build": {
    "dockerfilePath": "Dockerfile.api"
  },
  "deploy": {
    "startCommand": "gunicorn --bind 0.0.0.0:$PORT --workers 2 src.api.app:app",
    "healthcheckPath": "/api/v1/metadata"
  },
  "variables": {
    "FLASK_APP": "src/api/app.py"
  }
}
